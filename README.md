# CodeAlpha-WebPageTitleExtractor
A Python script to extract the title of a fixed webpage and save it to a text file. The script fetches the HTML content of a webpage, extracts the content inside the &lt;title> tag using regular expressions, and writes it to a .txt file in the same folder. Key concepts used: requests, re, os, file handling.
